[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libsndfile"]

[phases.install]
cmds = ["cd backend && pip install -e ."]

[phases.build]
cmds = []

[start]
cmd = "cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
